import 'package:flutter/cupertino.dart';

class StreamView extends StatelessWidget {
  static const routeName = '/stream-view';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hello"),
    );
  }
}
